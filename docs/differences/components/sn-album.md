# sn-album

> 旧版组件：无（v2 新增）

**旧版状态**：v2 新增的相册/图片组组件，旧版无此组件。

**差异明细**：

- 新增属性：`urls`、`rowCount`、`maxRow`、`space`、`borderRadius`、`imageMode`、`lazyLoad`、`preview`、`showMore`、`previewActions`、`placeholderImage`、`placeholderIcon`、`placeholderColor`、`placeholderIconColor`、`placeholderIconSize`、`customStyle`、`customClass`
- 新增事件：`click`（点击图片时返回索引）
- 移除属性、更名、类型或默认值变化、行为变化、新增方法与插槽：无
