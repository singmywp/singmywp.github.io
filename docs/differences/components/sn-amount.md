# sn-amount
> 旧版组件：无（v2 新增）
**旧版状态**：旧版无此组件，SinleUI 2.0 新增的金额格式化展示组件。
**差异明细**：
- 新增属性：全部（value、currency、decimals、roundingMode、separator、separatorDigits、toUpper、type、fontSize、currencyFontSize、intFontSize、decimalFontSize、color、currencyColor、intColor、decimalColor、bold、lines、selectable 及 currency/int/decimal 三部分的 style/class 外部样式与 textClass、customStyle、customClass）
- 移除属性：无
- 更名：无
- 类型或默认值变化：无
- 行为变化：无（组件为 v2 全新提供：字符串级精确舍入（round/ceil/floor/trunc）、千分位分隔、中文大写金额、分部分字号颜色控制、type 语义配色、可选中等）
- 新增事件、方法、插槽：无（纯展示组件，无事件、无对外方法、无插槽）
