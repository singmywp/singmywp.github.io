# sn-footer

> 旧版组件：无（v2 新增）

**旧版状态**：SinleUI 1.1.6 无此组件。

**差异明细**：

- v2 新增组件，无旧版对比。
- 提供品牌 Logo（`logo`，类型 SnFooterLogo）、导航链接（`links`，类型 SnFooterLink）与版权文字（`text`）三段式页脚，点击链接/Logo 自动按 `openType` 路由跳转，并触发 `linkclick`/`logoclick` 事件。
