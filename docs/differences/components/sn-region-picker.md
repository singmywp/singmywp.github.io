# sn-region-picker

> 旧版组件：无（v2 新增）

**旧版状态**：v1.1.6 无此组件，v2 新增（配套类型 SnRegionLevel / SnRegionNode / SnRegionValue / SnRegionDetail）。

**差异明细**：

- 全部为新增：组件本身及 v-model（SnRegionValue 区划代码数组）、change/confirm/cancel 与抽屉动画全周期事件、open/close 方法、内置中国省市区数据、省/市/县（区）mode 组合、直辖市处理、customFilter 过滤、级联联动、懒渲染等均为 v2 新增能力。
