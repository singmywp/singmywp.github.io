# sn-tree

> 旧版组件：无（v2 新增）

**旧版状态**：v1.1.6 无此组件，v2 新增（配套类型 SnTreeItem / SnTreeData / SnTreeLoadData / SnTreeLoadResolve / SnTreeLoadReject）。

**差异明细**：

- 全部为新增：组件本身及 v-model（String[] 选中 id）、change/expand/load-error 事件、expand/collapse 方法、prefix/suffix 作用域插槽、checkable/multiple/cascade/selectable/disableParentSelection 多模式选择、lazy 异步加载、节点级 icon/activeIcon/rotate 覆盖等均为 v2 新增能力。
